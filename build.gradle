// Root build.gradle - each subproject builds independently
// This file exists for IDE configuration only

plugins {
    id 'fabric-loom' version '1.6-SNAPSHOT' apply false
}

subprojects {
    apply plugin: 'fabric-loom'
}